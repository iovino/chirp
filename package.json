{
  "name": "chirp",
  "version": "0.1.0",
  "description": "Personal push-to-talk dictation: hold a key, speak, release — the text lands in whatever input has focus.",
  "main": "dist/main/index.js",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/copy-html.js",
    "start": "npm run build && electron .",
    "keys": "npm run build && electron . --log-keys"
  },
  "dependencies": {
    "uiohook-napi": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^37.2.0",
    "typescript": "^5.8.0"
  }
}
